Hier liegen die Workflows pro Stimme, exportiert aus ComfyUI über "Save (API Format)"
(vorher in den Einstellungen den Dev-Modus einschalten). Erwartet werden
`a.json` und `b.json`, siehe `../foxtts.example.json`.

Prüfen mit: `python foxtts.py probe voices/a.json`
