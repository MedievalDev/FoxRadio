Hier liegen die Workflows pro Stimme im API-Format: `a.json` und `b.json`
(nicht im Repo, weil sie Pfade und Referenztexte enthalten). Fertige Vorlagen
für die gängigen Qwen3-TTS-Node-Pakete liegen in `templates/`, Anleitung dort.

Prüfen mit: `python foxtts.py probe voices/a.json`
